/** @type {import('next').NextConfig} */
const nextConfig = {
  // Prototip .dc.html sayfaları geçiş döneminde /proto altında servis edilebilir (statik kopya).
  // SSR port'u tamamlandıkça bu ihtiyaç kalkar.
};
export default nextConfig;
